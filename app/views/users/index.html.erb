<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%# Rails knows to interpret @users as a list of User objects and iterates over it rendering each user using the _user.html.erb partial %>
	<%= render @users %>
</ul>

<%= will_paginate %>
